<h1><%= @doctor.name %></h1>

<h2>Appointments:</h2>
<ul>
  <% @doctor.appointments.each do |appointment| %>
    <li><%= link_to appointment.patient.name, patient_path(appointment.patient) %> on <%= appointment.readable_date %></li>
  <% end %>
</ul>
